🚛 САЙТ-ВИЗИТКА ДЛЯ ООО «АЛМИК» (Тверь)
ЧТО НУЖНО ИСПРАВИТЬ/ДОБАВИТЬ:
Все реквизиты компании (ИНН, ОГРН, юр. адрес) — это повышает доверие

Рабочие кнопки (все ссылки и формы)

Локализация под Тверь (упоминания города)

Реальная карта с адресом (ул. Седова, д. 55, кв. 80)

Адаптация под любые устройства

Эффекты и анимация (конфетка)

ГОТОВЫЙ КОД ДЛЯ REPLIT (КОПИРУЙ ЦЕЛИКОМ)
html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.5">
    <title>ООО «АЛМИК» — грузоперевозки в Твери и области</title>
    <!-- Шрифты -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome для иконок -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Анимации -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" />
    <style>
        /* ===== ОБНУЛЕНИЕ И БАЗОВЫЕ СТИЛИ ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Open Sans', sans-serif;
            color: #1e293b;
            line-height: 1.6;
            overflow-x: hidden;
            background-color: #f8fafc;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            line-height: 1.2;
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 30px;
        }

        /* ===== ЦВЕТОВАЯ СХЕМА ===== */
        :root {
            --primary: #0b3b5c;      /* Темно-синий (корпоративный) */
            --secondary: #e67e22;    /* Оранжевый (акцент) */
            --accent: #f39c12;        /* Ярко-оранжевый */
            --dark: #0a2a3b;
            --light: #f8fafc;
            --gray: #64748b;
            --success: #27ae60;
            --white: #ffffff;
            --black: #1e293b;
        }

        /* ===== АНИМАЦИИ ===== */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .animate-fade {
            animation: fadeInUp 0.8s ease-out;
        }

        .pulse-hover:hover {
            animation: pulse 0.5s ease-in-out;
        }

        /* ===== ШАПКА (ХЕДЕР) ===== */
        .header {
            background-color: var(--primary);
            padding: 15px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            border-bottom: 3px solid var(--secondary);
        }

        .header .container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .logo {
            display: flex;
            flex-direction: column;
        }

        .logo h1 {
            font-size: 32px;
            font-weight: 800;
            color: var(--white);
            letter-spacing: 1px;
            line-height: 1;
        }

        .logo h1 span {
            color: var(--secondary);
        }

        .logo .slogan {
            font-size: 12px;
            color: rgba(255,255,255,0.7);
            letter-spacing: 1px;
            margin-top: 5px;
        }

        .header-contacts {
            display: flex;
            align-items: center;
            gap: 30px;
        }

        .phone-wrapper {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
        }

        .phone-number {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 24px;
            color: var(--white);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: color 0.3s;
        }

        .phone-number i {
            font-size: 20px;
            color: var(--secondary);
        }

        .phone-number:hover {
            color: var(--secondary);
        }

        .work-time {
            font-size: 13px;
            color: rgba(255,255,255,0.6);
        }

        .callback-btn {
            background: var(--secondary);
            color: var(--white);
            border: none;
            padding: 14px 28px;
            border-radius: 50px;
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s;
            border: 2px solid var(--secondary);
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 4px 15px rgba(230, 126, 34, 0.3);
        }

        .callback-btn i {
            font-size: 16px;
        }

        .callback-btn:hover {
            background: transparent;
            color: var(--white);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(230, 126, 34, 0.4);
        }

        /* ===== МОБИЛЬНОЕ МЕНЮ ===== */
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            color: var(--white);
            font-size: 28px;
            cursor: pointer;
        }

        @media (max-width: 992px) {
            .header .container {
                flex-direction: column;
                gap: 15px;
            }
            
            .header-contacts {
                width: 100%;
                justify-content: center;
                flex-wrap: wrap;
            }
            
            .phone-wrapper {
                align-items: center;
            }
        }

        @media (max-width: 768px) {
            .mobile-menu-btn {
                display: block;
            }
            
            .header-contacts {
                display: none;
                flex-direction: column;
            }
            
            .header-contacts.active {
                display: flex;
            }
        }

        /* ===== ПЕРВЫЙ ЭКРАН (ГЕРОЙ) ===== */
        .hero {
            background: linear-gradient(135deg, var(--primary) 0%, #1a4a6e 100%);
            padding: 100px 0;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('https://images.unsplash.com/photo-1601584115197-04ecc0da31d7?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80') center/cover;
            opacity: 0.1;
            animation: fadeInUp 1.5s ease-out;
        }

        .hero .container {
            position: relative;
            z-index: 2;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            align-items: center;
        }

        .hero-content {
            color: var(--white);
        }

        .hero-content h1 {
            font-size: 52px;
            font-weight: 800;
            margin-bottom: 20px;
            line-height: 1.2;
            text-shadow: 0 5px 20px rgba(0,0,0,0.3);
            animation: fadeInUp 0.8s ease-out;
        }

        .hero-content .highlight {
            color: var(--secondary);
            border-bottom: 3px solid var(--secondary);
            display: inline-block;
        }

        .hero-subtitle {
            font-size: 20px;
            margin-bottom: 30px;
            opacity: 0.9;
            animation: fadeInUp 0.8s ease-out 0.2s both;
        }

        .hero-features {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 40px;
            animation: fadeInUp 0.8s ease-out 0.4s both;
        }

        .hero-feature {
            display: flex;
            align-items: center;
            gap: 10px;
            background: rgba(255,255,255,0.1);
            padding: 10px 20px;
            border-radius: 50px;
            backdrop-filter: blur(5px);
        }

        .hero-feature i {
            color: var(--secondary);
            font-size: 18px;
        }

        .hero-buttons {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
            animation: fadeInUp 0.8s ease-out 0.6s both;
        }

        .btn-primary {
            background: var(--secondary);
            color: var(--white);
            border: none;
            padding: 18px 40px;
            border-radius: 60px;
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s;
            border: 2px solid var(--secondary);
            display: inline-flex;
            align-items: center;
            gap: 12px;
            box-shadow: 0 10px 30px rgba(230, 126, 34, 0.3);
        }

        .btn-primary i {
            transition: transform 0.3s;
        }

        .btn-primary:hover {
            background: transparent;
            color: var(--white);
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(230, 126, 34, 0.4);
        }

        .btn-primary:hover i {
            transform: translateX(5px);
        }

        .btn-secondary {
            background: transparent;
            color: var(--white);
            border: 2px solid var(--white);
            padding: 18px 40px;
            border-radius: 60px;
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 12px;
        }

        .btn-secondary:hover {
            background: var(--white);
            color: var(--primary);
            transform: translateY(-3px);
        }

        .hero-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            background: var(--white);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 30px 60px rgba(0,0,0,0.1);
            animation: fadeInUp 0.8s ease-out 0.8s both;
        }

        .stat-item {
            text-align: center;
            border-right: 2px solid #eef2f6;
            padding: 0 15px;
        }

        .stat-item:last-child {
            border-right: none;
        }

        .stat-number {
            font-size: 42px;
            font-weight: 800;
            color: var(--primary);
            line-height: 1;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 14px;
            color: var(--gray);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        @media (max-width: 992px) {
            .hero .container {
                grid-template-columns: 1fr;
                text-align: center;
            }
            
            .hero-features {
                justify-content: center;
            }
            
            .hero-buttons {
                justify-content: center;
            }
            
            .stat-number {
                font-size: 32px;
            }
        }

        @media (max-width: 576px) {
            .hero-content h1 {
                font-size: 36px;
            }
            
            .hero-stats {
                grid-template-columns: 1fr;
            }
            
            .stat-item {
                border-right: none;
                border-bottom: 2px solid #eef2f6;
                padding-bottom: 15px;
            }
            
            .stat-item:last-child {
                border-bottom: none;
            }
        }

        /* ===== СЕКЦИЯ О КОМПАНИИ ===== */
        .about {
            padding: 100px 0;
            background: var(--white);
        }

        .about-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
        }

        .about-content h2 {
            font-size: 42px;
            color: var(--primary);
            margin-bottom: 20px;
            position: relative;
            padding-bottom: 20px;
        }

        .about-content h2::after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 80px;
            height: 4px;
            background: var(--secondary);
        }

        .about-text {
            margin-bottom: 30px;
            color: var(--gray);
            font-size: 16px;
            line-height: 1.8;
        }

        .company-details {
            background: var(--light);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 30px;
            border-left: 5px solid var(--secondary);
        }

        .detail-item {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
        }

        .detail-item:last-child {
            margin-bottom: 0;
        }

        .detail-icon {
            width: 50px;
            height: 50px;
            background: var(--secondary);
            color: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            flex-shrink: 0;
        }

        .detail-content h4 {
            font-size: 18px;
            color: var(--primary);
            margin-bottom: 5px;
        }

        .detail-content p {
            color: var(--gray);
            font-size: 16px;
        }

        .detail-content .small {
            font-size: 14px;
            color: #94a3b8;
        }

        .about-image {
            position: relative;
        }

        .about-image img {
            width: 100%;
            border-radius: 30px;
            box-shadow: 0 30px 60px rgba(0,0,0,0.1);
        }

        .experience-badge {
            position: absolute;
            bottom: -20px;
            right: -20px;
            background: var(--secondary);
            color: var(--white);
            padding: 30px;
            border-radius: 50%;
            width: 150px;
            height: 150px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            box-shadow: 0 20px 40px rgba(230, 126, 34, 0.3);
            animation: pulse 2s infinite;
        }

        .experience-badge .years {
            font-size: 48px;
            font-weight: 800;
            line-height: 1;
        }

        .experience-badge .text {
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
        }

        @media (max-width: 992px) {
            .about-grid {
                grid-template-columns: 1fr;
            }
            
            .experience-badge {
                width: 120px;
                height: 120px;
                padding: 20px;
            }
            
            .experience-badge .years {
                font-size: 36px;
            }
        }

        /* ===== ПРЕИМУЩЕСТВА ===== */
        .benefits {
            padding: 100px 0;
            background: linear-gradient(135deg, #f8fafc 0%, #eef2f6 100%);
        }

        .section-title {
            text-align: center;
            font-size: 42px;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .section-subtitle {
            text-align: center;
            font-size: 18px;
            color: var(--gray);
            margin-bottom: 60px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
        }

        .benefit-card {
            background: var(--white);
            padding: 40px 25px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            transition: all 0.3s;
            border: 1px solid transparent;
            position: relative;
            overflow: hidden;
        }

        .benefit-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: var(--secondary);
            transform: translateX(-100%);
            transition: transform 0.3s;
        }

        .benefit-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(230, 126, 34, 0.15);
            border-color: rgba(230, 126, 34, 0.1);
        }

        .benefit-card:hover::before {
            transform: translateX(0);
        }

        .benefit-icon {
            width: 80px;
            height: 80px;
            background: rgba(230, 126, 34, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            font-size: 36px;
            color: var(--secondary);
            transition: all 0.3s;
        }

        .benefit-card:hover .benefit-icon {
            background: var(--secondary);
            color: var(--white);
        }

        .benefit-card h3 {
            font-size: 22px;
            color: var(--primary);
            margin-bottom: 15px;
        }

        .benefit-card p {
            color: var(--gray);
            font-size: 15px;
            line-height: 1.7;
        }

        @media (max-width: 1200px) {
            .benefits-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 576px) {
            .benefits-grid {
                grid-template-columns: 1fr;
            }
        }

        /* ===== УСЛУГИ ===== */
        .services {
            padding: 100px 0;
            background: var(--white);
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 25px;
            margin-top: 50px;
        }

        .service-card {
            display: flex;
            align-items: center;
            gap: 20px;
            padding: 25px;
            background: var(--light);
            border-radius: 15px;
            transition: all 0.3s;
            border: 1px solid transparent;
        }

        .service-card:hover {
            background: var(--white);
            border-color: var(--secondary);
            box-shadow: 0 15px 30px rgba(230, 126, 34, 0.1);
            transform: scale(1.02);
        }

        .service-icon {
            width: 60px;
            height: 60px;
            background: var(--secondary);
            color: var(--white);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            flex-shrink: 0;
        }

        .service-content h3 {
            font-size: 20px;
            color: var(--primary);
            margin-bottom: 8px;
        }

        .service-content p {
            color: var(--gray);
            font-size: 14px;
        }

        @media (max-width: 768px) {
            .services-grid {
                grid-template-columns: 1fr;
            }
        }

        /* ===== КАК МЫ РАБОТАЕМ ===== */
        .process {
            padding: 100px 0;
            background: linear-gradient(135deg, var(--primary) 0%, #1a4a6e 100%);
            color: var(--white);
        }

        .process .section-title {
            color: var(--white);
        }

        .process .section-subtitle {
            color: rgba(255,255,255,0.8);
        }

        .process-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            margin-top: 50px;
            position: relative;
        }

        .process-step {
            text-align: center;
            position: relative;
            padding: 40px 30px;
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.2);
            transition: all 0.3s;
        }

        .process-step:hover {
            transform: translateY(-10px);
            background: rgba(255,255,255,0.15);
            border-color: var(--secondary);
        }

        .step-number {
            width: 70px;
            height: 70px;
            background: var(--secondary);
            color: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            font-weight: 800;
            margin: 0 auto 25px;
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }

        .process-step h3 {
            font-size: 24px;
            margin-bottom: 15px;
        }

        .process-step p {
            opacity: 0.9;
            font-size: 16px;
            line-height: 1.7;
        }

        .step-arrow {
            position: absolute;
            right: -20px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 32px;
            color: var(--secondary);
            z-index: 10;
        }

        @media (max-width: 992px) {
            .process-grid {
                grid-template-columns: 1fr;
            }
            
            .step-arrow {
                display: none;
            }
        }

        /* ===== ФОРМА СВЯЗИ ===== */
        .contact-section {
            padding: 100px 0;
            background: var(--white);
        }

        .contact-wrapper {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            background: var(--light);
            border-radius: 40px;
            padding: 50px;
            box-shadow: 0 30px 60px rgba(0,0,0,0.1);
        }

        .contact-info h3 {
            font-size: 32px;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .contact-info p {
            color: var(--gray);
            margin-bottom: 30px;
            font-size: 16px;
            line-height: 1.8;
        }

        .info-items {
            display: flex;
            flex-direction: column;
            gap: 25px;
        }

        .info-item {
            display: flex;
            gap: 15px;
            align-items: flex-start;
        }

        .info-icon {
            width: 50px;
            height: 50px;
            background: var(--secondary);
            color: var(--white);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            flex-shrink: 0;
        }

        .info-text h4 {
            font-size: 18px;
            color: var(--primary);
            margin-bottom: 5px;
        }

        .info-text a {
            color: var(--gray);
            text-decoration: none;
            transition: color 0.3s;
            font-size: 16px;
            display: block;
        }

        .info-text a:hover {
            color: var(--secondary);
        }

        .contact-form {
            background: var(--white);
            padding: 40px;
            border-radius: 30px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.05);
        }

        .form-group {
            margin-bottom: 20px;
        }
